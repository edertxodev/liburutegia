<table class="table table-bordered table-hover">
    <tr>
        <th>Libro</th>
        <th>Usuario</th>
        <th>Fecha solicitud</th>
        <th>Aceptar</th>
    </tr>
      <% @solicitudes.where(aceptado: false).each do |s| %>
      <tr>
        <td><%= s.libro %></td>
        <td><%= s.username %></td>
        <td><%= s.created_at %></td>
        <td><%= link_to image_tag('check.png'), {:controller => 'solicitudes', :action => 'aceptar_solicitud', :id => s.id} %></td>
      </tr>
      <% end %>
  </table>
  <%= will_paginate @solicitudes, renderer: BootstrapPagination::Rails, :next_label => '&raquo;', :previous_label => '&laquo;' %>